import { Typography } from "@material-ui/core";
import Popup from "./Popup";

export default function NotificationsPopover(props) {
  return (
    <Popup {...props}>
      <Typography>Notifications Center Notifications</Typography>
      <Typography>Notifications Center Notifications</Typography>
      <Typography>Notifications Center Notifications</Typography>
      <Typography>Notifications Center Notifications</Typography>
      <Typography>Notifications Center Notifications</Typography>
      <Typography>Notifications Center Notifications</Typography>
      <Typography>Notifications Center Notifications</Typography>
    </Popup>
  );
}
